const currentStageIndex = state => state.currentStageIndex
const currentStage = state => state.currentStage
const currentQuestionSet = state => state.currentQuestionSet
const currentQuestion = state => state.currentQuestion
const currentQuestionIndex = state => state.currentQuestionIndex

export default {
  currentStageIndex,
  currentStage,
  currentQuestionSet,
  currentQuestion,
  currentQuestionIndex
}
